/*
segmentation fault/ core dump
- a specific kind of error caused by accessing memory that "does not belong to you"
- they are caused by a program trying to read or write an illegal memory location. a common condition that causes programs to crash
- an error returned by hardware with memory protection that tells the OS that memory violation has occurred
- the OS usually ,,,
*/

#include<stdio.h>
int main()
{
    int x;
}